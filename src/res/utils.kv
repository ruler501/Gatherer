<ManaImage>:
    size: sp(18), sp(18)
    canvas:
        Rectangle:
            texture: self.texture
            pos: self.pos
            size: sp(18), sp(18)

<CachedImage>:
    id: image
    canvas.before:
        Rectangle:
            texture: image.texture
            pos: image.pos
            size: image.size
